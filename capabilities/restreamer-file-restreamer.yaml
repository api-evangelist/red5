naftiko: 1.0.0-alpha2
info:
  label: Red5 Pro Restreamer API — File Restreamer
  description: 'Red5 Pro Restreamer API — File Restreamer. 2 operations. Lead operation: Create File Restream Provision. Self-contained
    Naftiko capability covering one Red5 business surface.'
  tags:
  - Red5
  - File Restreamer
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    RED5_API_KEY: RED5_API_KEY
capability:
  consumes:
  - type: http
    namespace: restreamer-file-restreamer
    baseUri: http://{host}:5080
    description: Red5 Pro Restreamer API — File Restreamer business capability. Self-contained, no shared references.
    resources:
    - name: filerestreamer-provision.create
      path: /filerestreamer/provision.create
      operations:
      - name: createfilerestreamprovision
        method: POST
        description: Create File Restream Provision
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: filerestreamer-provision.delete
      path: /filerestreamer/provision.delete
      operations:
      - name: deletefilerestreamprovision
        method: POST
        description: Delete File Restream Provision
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: accessToken
      value: '{{env.RED5_API_KEY}}'
      placement: query
  exposes:
  - type: rest
    namespace: restreamer-file-restreamer-rest
    port: 8080
    description: REST adapter for Red5 Pro Restreamer API — File Restreamer. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/filerestreamer/provision-create
      name: filerestreamer-provision-create
      description: REST surface for filerestreamer-provision.create.
      operations:
      - method: POST
        name: createfilerestreamprovision
        description: Create File Restream Provision
        call: restreamer-file-restreamer.createfilerestreamprovision
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/filerestreamer/provision-delete
      name: filerestreamer-provision-delete
      description: REST surface for filerestreamer-provision.delete.
      operations:
      - method: POST
        name: deletefilerestreamprovision
        description: Delete File Restream Provision
        call: restreamer-file-restreamer.deletefilerestreamprovision
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: restreamer-file-restreamer-mcp
    port: 9090
    transport: http
    description: MCP adapter for Red5 Pro Restreamer API — File Restreamer. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: create-file-restream-provision
      description: Create File Restream Provision
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: restreamer-file-restreamer.createfilerestreamprovision
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-file-restream-provision
      description: Delete File Restream Provision
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: restreamer-file-restreamer.deletefilerestreamprovision
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
