naftiko: 1.0.0-alpha2
info:
  label: Red5 Pro Stream Manager 2.0 API — Provision
  description: 'Red5 Pro Stream Manager 2.0 API — Provision. 3 operations. Lead operation: Get Stream Provision. Self-contained
    Naftiko capability covering one Red5 business surface.'
  tags:
  - Red5
  - Provision
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    RED5_API_KEY: RED5_API_KEY
capability:
  consumes:
  - type: http
    namespace: stream-manager-2-provision
    baseUri: https://{streamManagerDomain}/as/v1
    description: Red5 Pro Stream Manager 2.0 API — Provision business capability. Self-contained, no shared references.
    resources:
    - name: streams-provision-nodeGroupName-streamGuid
      path: /streams/provision/{nodeGroupName}/{streamGuid}
      operations:
      - name: getstreamprovision
        method: GET
        description: Get Stream Provision
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createstreamprovision
        method: POST
        description: Create Stream Provision
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletestreamprovision
        method: DELETE
        description: Delete Stream Provision
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.RED5_API_KEY}}'
  exposes:
  - type: rest
    namespace: stream-manager-2-provision-rest
    port: 8080
    description: REST adapter for Red5 Pro Stream Manager 2.0 API — Provision. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/streams/provision/{nodegroupname}/{streamguid}
      name: streams-provision-nodegroupname-streamguid
      description: REST surface for streams-provision-nodeGroupName-streamGuid.
      operations:
      - method: GET
        name: getstreamprovision
        description: Get Stream Provision
        call: stream-manager-2-provision.getstreamprovision
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createstreamprovision
        description: Create Stream Provision
        call: stream-manager-2-provision.createstreamprovision
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletestreamprovision
        description: Delete Stream Provision
        call: stream-manager-2-provision.deletestreamprovision
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: stream-manager-2-provision-mcp
    port: 9090
    transport: http
    description: MCP adapter for Red5 Pro Stream Manager 2.0 API — Provision. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: get-stream-provision
      description: Get Stream Provision
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: stream-manager-2-provision.getstreamprovision
      outputParameters:
      - type: object
        mapping: $.
    - name: create-stream-provision
      description: Create Stream Provision
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: stream-manager-2-provision.createstreamprovision
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-stream-provision
      description: Delete Stream Provision
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: stream-manager-2-provision.deletestreamprovision
      outputParameters:
      - type: object
        mapping: $.
